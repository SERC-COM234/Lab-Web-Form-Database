/**************************************************************
This file should contain the SQL queries to:
- create a database called WebForm
- create a table called Submission with the following columns:
    - id (int, primary key, auto increment)
    - name (varchar(50), not null)
    - email (varchar(50), not null)
    - message (varchar(max), not null)
**************************************************************/
